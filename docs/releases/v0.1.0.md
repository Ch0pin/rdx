First public RDX release (alpha): a native Rust APK/DEX decompiler for macOS, Windows and Linux.

### Included

- Java reconstruction with explicit DEX fallback for unsupported methods.
- Code/resource search, declaration navigation, usages, callers/callees, subclasses and implementations.
- Decoded manifests/XML, resource resolution, exported-component highlighting and asset previews.
- Light/dark themes, bundled fonts, tabs/bookmarks and Frida snippet copying.
- Local native MCP server, GUI controls and isolated projects for parallel agents.

### Downloads

| Platform | Archive |
| --- | --- |
| Apple Silicon Mac | `rdx-v0.1.0-macos-arm64.zip` |
| Intel Mac | `rdx-v0.1.0-macos-x86_64.zip` |
| Windows x64 | `rdx-v0.1.0-windows-x86_64.zip` |
| Linux x64 | `rdx-v0.1.0-linux-x86_64.tar.gz` |

Extract the archive fully. On macOS move `RDX.app` to Applications; on Windows
open `rdx.exe`; on Linux run `./rdx`. Archives include runtime instructions,
third-party notices and the optional source-statistics plugin. `SHA256SUMS`
contains the archive checksums. GitHub's automatic source archives are for developers.

### Requirements and known limits

No Java, Python, Rust or Android SDK is needed for the prebuilt apps.
macOS 13+, Windows 10+ x64, or Linux x64 with glibc 2.35+ and a graphical desktop
are required. Linux needs OpenGL/X11 or Wayland libraries and a desktop portal
for the file picker. Windows uses the static MSVC runtime.

macOS builds are ad-hoc signed but not notarized; Windows builds are unsigned.
First launch may require OS approval. See the [installation guide](https://github.com/Ch0pin/rdx/blob/v0.1.0/docs/install.md)
for platform instructions and MCP setup.

The native engine remains alpha: reconstruction coverage is incomplete and
unsupported methods are displayed as DEX disassembly. This is not a claim of
100% semantic accuracy. MCP's current tool set is documented [here](https://github.com/Ch0pin/rdx/blob/v0.1.0/docs/mcp.md).
